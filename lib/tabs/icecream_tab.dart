import 'package:flutter/material.dart';

class IcecreamTab extends StatelessWidget {
  const IcecreamTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("icecream tab"),);
  }
}